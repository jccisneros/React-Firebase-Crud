import styled from '@emotion/styled'

export const Form = styled.form`
  background-color: ${ props => props.theme.colors.primary };
  padding: 2rem;
  margin: 2rem 0;
  border-radius: 15px;
  box-shadow: 8px 10px 40px 10px rgba( 0, 0, 0, 0.2);
`